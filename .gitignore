# Python
backend/.venv/
__pycache__/
*.pyc
backend/.env

# Data (rendered pages, originals, results)
backend/data/

# Generated sample ground-truth (regenerated by make_samples.py alongside the PDFs)
backend/samples/ground_truth.json

# Node
frontend/node_modules/
frontend/dist/

# Misc
.DS_Store

# Orphaned demo runtime left after de-duplication — the app now lives under
# leverantorsreskontra/. These top-level dirs hold only untracked venv/
# node_modules/data from the running demo; ignore them on this branch. Safe to
# physically delete once the demo servers are stopped (reclaims ~300M, but
# removes the demo's ingested data, which is not in git).
/backend/
/frontend/
/docs/
